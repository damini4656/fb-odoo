name: Poll Facebook Leads

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger poll
        run: |
          curl -s --max-time 300 https://fb-odoo-ruddy.vercel.app/poll
